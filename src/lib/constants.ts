/** How long after creation a pitch can be edited (in milliseconds). */
export const EDIT_WINDOW_MS = 5 * 60 * 1000;

/** Maximum page number allowed for paginated queries. */
export const MAX_PAGE = 1000;
